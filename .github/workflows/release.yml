name: Build & Release Electron Apps

on:
  push:
    tags:
      - 'v*'               # Trigger on version tags: v1.0.0, v1.2.3, etc.
  workflow_dispatch:        # Allow manual trigger from GitHub UI
    inputs:
      draft:
        description: 'Create as draft release?'
        required: false
        default: 'true'
        type: boolean

jobs:
  # ─────────────────────────────────────────────────────────────────────────────
  # macOS Build — produces .dmg and .pkg
  # ─────────────────────────────────────────────────────────────────────────────
  build-mac:
    name: Build macOS
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build frontend
        run: npm run build:ui

      - name: Build macOS (dmg + pkg)
        # electron-builder rebuilds native modules internally (better-sqlite3 etc.)
        # --publish never prevents electron-builder auto-publishing via git tag detection
        # (publishing is handled by the dedicated `release` job below)
        run: npx electron-builder --mac --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false   # skip code signing

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mac-build
          path: |
            dist/*.dmg
            dist/*.pkg
          if-no-files-found: error
          retention-days: 5

  # ─────────────────────────────────────────────────────────────────────────────
  # Windows Build — produces .exe installer
  # ─────────────────────────────────────────────────────────────────────────────
  build-windows:
    name: Build Windows
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build frontend
        run: npm run build:ui

      - name: Build Windows (NSIS installer)
        # --publish never prevents electron-builder auto-publishing via git tag detection
        run: npx electron-builder --win --publish never
        env:
          CSC_LINK: ''           # skip code signing
          CSC_KEY_PASSWORD: ''

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: |
            dist/*.exe
            dist/*.blockmap
          if-no-files-found: error
          retention-days: 5

  # ─────────────────────────────────────────────────────────────────────────────
  # Create GitHub Release and attach all built assets
  # Runs after BOTH platform builds succeed
  # ─────────────────────────────────────────────────────────────────────────────
  release:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: [build-mac, build-windows]
    permissions:
      contents: write    # Required to create releases and upload assets

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          name: mac-build
          path: release-assets

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: windows-build
          path: release-assets

      - name: List release assets
        run: ls -lh release-assets/

      - name: Extract Extact App Version
        id: version
        run: echo "APP_VERSION=v$(node -p "require('./package.json').version")" >> $GITHUB_ENV

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.APP_VERSION }}
          name: AJM WhatsApp Bot ${{ env.APP_VERSION }}
          draft: ${{ github.event.inputs.draft || 'false' }}
          prerelease: ${{ contains(env.APP_VERSION, '-beta') || contains(env.APP_VERSION, '-rc') }}
          generate_release_notes: true
          files: release-assets/*
          body: |
            ## 📦 Downloads

            | Platform | File | Description |
            |----------|------|-------------|
            | 🍎 macOS | \`*.dmg\` | Drag-and-drop installer |
            | 🍎 macOS | \`*.pkg\` | Package installer |
            | 🪟 Windows | \`*.exe\` | NSIS setup wizard |

            ## 🚀 Getting Started
            See [docs/INSTALL.md](docs/INSTALL.md) for setup instructions.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
